{
  "name": "@hubris/abis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate": "node ./scripts/generate.mjs",
    "dev": "pnpm run generate && pnpm run build",
    "test": "echo 'No tests for @hubris/abis'"
  },
  "devDependencies": {
    "@types/node": "^22.8.0"
  }
}
