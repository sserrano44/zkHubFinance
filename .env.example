# Hub (Ethereum mainnet)
HUB_CHAIN_ID=1
HUB_RPC_URL=http://127.0.0.1:8545
# Optional admin RPC for tenderly_setBalance in fork E2E.
# HUB_ADMIN_RPC_URL=

# Active spoke selector.
SPOKE_NETWORK=base

# Spoke network catalog
SPOKE_BASE_CHAIN_ID=8453
SPOKE_BASE_RPC_URL=http://127.0.0.1:9545
SPOKE_BASE_ADMIN_RPC_URL=
SPOKE_BSC_CHAIN_ID=56
SPOKE_BSC_RPC_URL=https://virtual.binance.eu.rpc.tenderly.co/b6772c9b-23b5-42c5-a5e6-eda3969b5711
SPOKE_BSC_ADMIN_RPC_URL=

# Generated by deploy-local.sh for local runs:
# HUB_LOCK_MANAGER_ADDRESS=
# HUB_SETTLEMENT_ADDRESS=
# HUB_CUSTODY_ADDRESS=
# SPOKE_PORTAL_ADDRESS=
# SPOKE_TO_HUB_TOKEN_MAP={...}

INDEXER_API_URL=http://127.0.0.1:3030
RELAYER_API_URL=http://127.0.0.1:3040
PROVER_API_URL=http://127.0.0.1:3050

# Internal service authentication (indexer/prover internal routes)
INTERNAL_API_AUTH_SECRET=dev-internal-auth-secret
# Optional previous secret during key rotation window.
# INTERNAL_API_AUTH_PREVIOUS_SECRET=
# Service identity used when signing internal requests.
# INTERNAL_API_SERVICE_NAME=relayer
# Verifier allowlist for x-elhub-internal-service on /internal/* routes.
INTERNAL_API_ALLOWED_SERVICES=relayer,prover,e2e
# Optional explicit IP allowlist for /internal/* routes (comma-separated).
# INTERNAL_API_ALLOWED_IPS=10.0.0.10,10.0.0.11
# Require private client IPs for /internal/* routes when no allowlist is set.
# Defaults to 1 in production, 0 otherwise.
# INTERNAL_API_REQUIRE_PRIVATE_IP=1
# Enable Express trust proxy for internal IP checks behind ingress proxies.
# INTERNAL_API_TRUST_PROXY=0
# Set ZKHUB_ENV=production to enforce fail-closed startup checks.
# ZKHUB_ENV=production
# In production, set a specific origin instead of '*'.
CORS_ALLOW_ORIGIN=*

# Persistence backends for P0-4 durability rollout.
# Indexer: json (legacy) or sqlite (transactional).
INDEXER_DB_KIND=json
# INDEXER_DB_PATH=/abs/path/indexer.db

# Prover queue/state: json (legacy) or sqlite (transactional).
PROVER_STORE_KIND=json
# PROVER_DB_PATH=/abs/path/prover.db
# Legacy JSON paths (used when PROVER_STORE_KIND=json):
# PROVER_QUEUE_PATH=/abs/path/prover-queue.json
# PROVER_STATE_PATH=/abs/path/prover-state.json

# Fork E2E gas targets used by scripts/e2e-fork.mjs.
# E2E_MIN_OPERATOR_GAS_ETH=0.05
# E2E_MIN_PROVER_GAS_ETH=0.1
# E2E_PROVER_GAS_BUFFER_ETH=0.01

NEXT_PUBLIC_HUB_RPC_URL=http://127.0.0.1:8545
NEXT_PUBLIC_SPOKE_RPC_URL=http://127.0.0.1:9545
NEXT_PUBLIC_HUB_CHAIN_ID=1
NEXT_PUBLIC_SPOKE_CHAIN_ID=8453
NEXT_PUBLIC_SPOKE_NETWORK=base
NEXT_PUBLIC_RELAYER_API_URL=http://127.0.0.1:3040
NEXT_PUBLIC_INDEXER_API_URL=http://127.0.0.1:3030
